MISS FANG EMBEDDED VOICE — LOCAL + GITHUB FIX

Open index.html.

The Call My Parent game now contains Miss Fang's real voice directly inside the HTML file.
There is no speech-synthesis fallback and no external audio folder is required.
This version is designed to work when opened locally with file:// and when uploaded to GitHub Pages.

Important: upload the complete folder because the Number Rope Battle still uses its own assets folder.
